href - hypertext reference

convention is to use a hyphen for class/ id for 2 word. ex: course-image

In CSS, setting font element in the "body" element will apply the font to all child elements under the body element. We call this the properties are inherited from parent element. However, it doesn't work for all properties.

In CSS, fill area includes the padding and the margin. This area is used when we apply background color and background images.

